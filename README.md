# QTIM тестовое задание - фронт

## О проекте

Тестовое задание представляет собой реализацию блога с использованием Nuxt 3 и TypeScript. Проект демонстрирует современные практики разработки веб-приложений с использованием Vue 3 Composition API.

### Демо

- 📂 [GitHub репозиторий](https://github.com/scoutrul/qtim)

Рабочая версия приложения доступна по ссылке: [QTIM Test App](https://qtim-testovoe.vercel.app/)
Storybook доступен по адресу: [QTIM Storybook](https://qtim-testovoe.vercel.app/storybook)

### Основные возможности

📄 Вывод списка статей с пагинацией

- Динамическая пагинация с бесконечной прокруткой
- Кэширование загруженных данных
- Оптимизированная загрузка при скролле

🔍 Детальные страницы статей

- Динамические мета-теги для SEO
- Автоматическая генерация заголовков страниц
- Оптимизированные Open Graph теги для соцсетей

🎭 SEO и мета-теги

- Динамические заголовки для каждой страницы
- Автоматическая генерация description для статей
- Open Graph теги для корректного шеринга в соцсетях
- Кастомный фавикон в формате SVG

🛡 Обработка ошибок

- Кастомная страница 404 для несуществующих страниц
- Обработка 500 ошибок и других серверных ошибок
  - Информативные сообщения с кодом ошибки
  - Возможность перезагрузки страницы
  - Автоматический редирект на главную при критических ошибках
  - Сохранение состояния приложения при восстановлении
  - Логирование ошибок для отладки
- Динамические заголовки для страниц ошибок
- Возможность быстрого возврата на главную
- Анимированные компоненты для улучшения UX
- Обработка таймаутов запросов
- Повторные попытки при временных сбоях сети
- Graceful degradation при отсутствии соединения

🔄 Состояния загрузки

- Скелетоны для статей и контента
- Плейсхолдеры для изображений
- Плавные анимации появления контента

🌍 Мультиязычность

- Поддержка русского и английского языков
- Переключатель языка в шапке сайта
- Локализованные сообщения об ошибках

📱 Адаптивность и доступность

- Адаптивная верстка для всех устройств
- Принудительная портретная ориентация на мобильных
- Оптимизированные размеры карточек
- Доступность с клавиатуры

🎨 UI/UX особенности

- Современный минималистичный дизайн
- Анимации и hover-эффекты
- Кнопка "Вверх" для удобной навигации
- Форма обратной связи
- Принудительная портретная ориентация на мобильных устройствах
  - Автоматическое определение ориентации устройства
  - Всплывающее уведомление при горизонтальной ориентации
  - Блокировка контента до поворота устройства
  - Плавная анимация появления/исчезновения предупреждения

⚡️ Оптимизация

- Ленивая загрузка изображений
- Оптимизированные шрифты
- Кэширование данных
- Минимизация ререндеров

🖼 Заглушки для изображений, если они не загружаются

🌍 Смена языка (EN/RU)

📱 Адаптивная вёрстка

- Сетка меняется на мобильных устройствах
- Принудительная портретная ориентация на мобильных
- Оптимизированные размеры карточек

🎨 Брендинг и идентификация

- Векторный фавикон в виде логотипа
- Web App Manifest для установки на устройства
- Консистентные мета-теги для всех страниц

🔝 Кнопка "Наверх"

📩 Форма обратной связи

❌ Страница ошибки 404

🎭 Анимации и hover-эффекты для интерактивности

### Storybook

Проект включает в себя Storybook - инструмент для разработки и тестирования UI компонентов в изоляции. Storybook позволяет:

- Разрабатывать компоненты независимо от основного приложения
- Тестировать различные состояния компонентов
- Документировать компоненты и их использование
- Создавать интерактивные примеры использования компонентов
- Проверять адаптивность компонентов
- Тестировать доступность компонентов

В Storybook доступны следующие компоненты:

- **Typography** - Компоненты типографики (заголовки, параграфы, ссылки)

Storybook доступен по адресу `/storybook` на деплоенном сайте (например, `https://qtim-testovoe.vercel.app/storybook`).

### API Endpoints:

- Список статей: `https://6082e3545dbd2c001757abf5.mockapi.io/qtim-test-work/posts/`
- Детальная страница: `https://6082e3545dbd2c001757abf5.mockapi.io/qtim-test-work/posts/${id}`

### Дизайн

Макет доступен в [Figma](https://www.figma.com/file/YuLYXnZmLk1U5sKAYjQ0Qy/Test?type=design&node-id=0-1&mode=design&t=qUknJKzxfWOznHn0-0)

## Технологический стек

### Основные технологии:

- **Nuxt 3** - Фреймворк для создания Vue.js приложений
- **Vue 3** - JavaScript фреймворк для создания пользовательских интерфейсов
- **TypeScript** - Типизированный JavaScript
- **Composition API** - Современный подход к организации логики компонентов
- **Vue Router** - Официальная библиотека маршрутизации для Vue.js
- **@vueuse/core** - Коллекция полезных Vue Composition API утилит

### Стилизация и UI:

- **Tailwind CSS** - Utility-first CSS фреймворк
- **PostCSS** - Инструмент для трансформации CSS
- **Autoprefixer** - Плагин для добавления вендорных префиксов
- **@tailwindcss/typography** - Плагин для типографики
- **@nuxtjs/i18n** - Модуль для интернационализации

### Инструменты разработки:

- **ESLint** - Линтер для JavaScript/TypeScript с плагинами:
  - @typescript-eslint/eslint-plugin
  - @typescript-eslint/parser
  - eslint-config-prettier
  - eslint-plugin-prettier
  - eslint-plugin-vue
- **Prettier** - Форматтер кода
- **Vite** - Сборщик и dev-сервер
  - @vitejs/plugin-vue
  - vite-tsconfig-paths
- **Vitest** - Фреймворк для тестирования
  - @vitest/coverage-v8
  - @vue/test-utils
  - jsdom для тестового окружения
- **Husky** - Инструмент для настройки Git hooks
- **lint-staged** - Запуск линтеров только на измененных файлах
- **TypeScript** - Система типов с конфигурацией:
  - vue-tsc
  - @nuxt/typescript-build
  - @vue/eslint-config-typescript

### Управление конфигурациями и безопасность:

- **Переменные окружения (.env)** - Безопасное хранение API URL и конфигураций
- **Валидация env** - Проверка обязательных переменных окружения при запуске
- **Централизованные константы** - Использование typesafe констант для UI элементов
- **API конфигурация** - Централизованное управление API эндпоинтами
- **TypeScript типы** - Строгая типизация всего кода для надежности

### Тестирование:

- **Vitest** - Современный фреймворк для тестирования
- **Vue Test Utils** - Официальная библиотека для тестирования Vue компонентов
- **Покрытие кода** - Анализ покрытия кода тестами с @vitest/coverage-v8
- **Моковые данные** - Использование фейковых данных для тестирования без API
- **Среда тестирования** - JSDOM для эмуляции браузерного окружения

### Система контроля качества кода:

- **Pre-commit хуки** - Автоматизированные проверки кода перед коммитом
  - Проверка форматирования кода
  - Проверка линтера на измененных файлах
  - Проверка типов TypeScript
  - Выполнение тестов (опционально)
- **Комплексная валидация** - Единая команда для проверки кода
  - Форматирование (Prettier)
  - Линтинг (ESLint)
  - Проверка типов (TypeScript)
  - Тестирование (Vitest)
- **Автоматическое исправление** - Автоматическое форматирование и исправление ошибок
  - Авто-форматирование кода (Prettier)
  - Авто-исправление ошибок линтера (ESLint)
- **Непрерывная проверка** - Контроль качества на всех этапах разработки
  - При изменении файлов
  - При коммите
  - При пуше
  - При сборке
- **Строгие правила** - Высокие стандарты качества кода
  - Строгий режим TypeScript
  - Обязательная обработка промисов
  - Строгие правила ESLint и TypeScript
  - Запрет небезопасного кода

## Установка и запуск проекта

### Установка зависимостей:

```bash
# Установка зависимостей
npm install
# или
yarn install
```

### Запуск в режиме разработки:

```bash
npm run dev
# или
yarn dev
```

### Запуск тестов:

```bash
# Запуск тестов
npm run test
# или
npm run test:coverage # Для получения отчета о покрытии
# или
npm run test:ui # Для запуска в UI режиме
```

### Проверка кода:

```bash
# Проверка форматирования
npm run format:check
# или
npm run lint:check # Проверка линтером
# или
npm run validate # Запуск всех проверок
```

### Автоматическое исправление:

```bash
# Исправление форматирования
npm run format
# или
npm run lint # Исправление ошибок линтера
# или
npm run lint:fix # Исправление линтера + форматирование
```

### Pre-commit проверки:

Проект настроен с использованием Husky и lint-staged для автоматической проверки кода перед коммитом:

1. Проверка форматирования и линтинг с помощью `lint-staged` для измененных файлов
2. Проверка типов TypeScript с помощью `vue-tsc --noEmit`

Эти проверки запускаются автоматически перед каждым коммитом и предотвращают попадание некачественного кода в репозиторий.

### Сборка для продакшена:

```bash
npm run build
# или
yarn build
```

### Предпросмотр продакшен-сборки:

```bash
npm run preview
# или
yarn preview
```

## Структура проекта

```
├── assets/          # Статические ресурсы (изображения, стили)
├── components/      # Vue компоненты
│   ├── article/    # Компоненты для статей
│   ├── contact/    # Компоненты формы обратной связи
│   ├── footer/     # Компоненты футера
│   ├── layout/     # Компоненты макета
│   ├── pages/      # Компоненты страниц
│   └── ui/         # Переиспользуемые UI компоненты
├── composables/     # Композабл функции
│   ├── useI18n.ts          # Логика интернационализации
│   ├── useContactModal.ts  # Логика модального окна контактов
│   ├── usePosts.ts         # Логика работы со статьями
│   ├── useScroll.ts        # Логика скролла
│   └── useSkeleton.ts      # Логика скелетонов
├── config/          # Файлы конфигурации
│   ├── api.ts       # Конфигурация API
│   └── env.ts       # Валидация переменных окружения
├── constants/       # Константы и конфигурации
│   └── article.ts   # Константы для компонентов статей
├── entities/        # Бизнес-сущности
├── layouts/         # Шаблоны страниц
├── pages/          # Маршруты приложения
│   ├── article/    # Страницы статей
│   ├── about.vue   # Страница о нас
│   ├── index.vue   # Главная страница
│   └── works.vue   # Страница работ
├── public/         # Публичные файлы
├── tests/          # Тесты
│   └── composables/  # Тесты композаблов
├── types/          # TypeScript типы
│   ├── components.d.ts  # Типы компонентов
│   ├── env.d.ts         # Типы для переменных окружения
│   ├── typography.ts    # Типы типографики
│   ├── colors.ts        # Типы цветов
│   └── post.ts          # Типы статей
└── utils/          # Вспомогательные функции
    └── api.ts      # Утилиты для работы с API

# Конфигурационные файлы
├── .env.example        # Пример .env файла с переменными окружения
├── .eslintrc.json      # Конфигурация ESLint
├── .husky/             # Git hooks
│   └── pre-commit      # Хук перед коммитом
├── .prettierrc         # Конфигурация Prettier
├── nuxt.config.ts      # Конфигурация Nuxt
├── tailwind.config.js  # Конфигурация Tailwind
└── tsconfig.json       # Конфигурация TypeScript
```

## Развертывание

Приложение развернуто на платформе Vercel. Автоматическое развертывание происходит при каждом push в main ветку.

## Контроль качества кода

### Автоматизация проверок

Проект использует Git hooks (Husky) и lint-staged для автоматизации проверок кода:

- **Pre-commit hook** - Перед каждым коммитом автоматически запускаются:
  - ESLint для проверки синтаксиса и правил кода
  - Prettier для форматирования кода
  - Проверка типов TypeScript

### Форматирование и линтинг

- **ESLint** настроен для работы с Vue 3, TypeScript и стандартами Prettier
- **Prettier** обеспечивает единообразное форматирование кода
- **TypeScript** с строгим режимом проверки типов
- **Vue/eslint-config-typescript** для интеграции TypeScript с Vue компонентами

### Типизация

Весь проект имеет строгую типизацию с использованием TypeScript:

- Интерфейсы для всех данных и пропсов компонентов
- Типы для переменных окружения
- Типизированные константы (as const)
- Строгая типизация API ответов
